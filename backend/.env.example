# Gemini is optional: no key uses deterministic requirement parsing.
# Put keys in priority order. The second is used only if the first API call fails,
# including quota/rate-limit failures. Do not commit your real .env file.
GEMINI_API_KEYS=first_key,second_key
GEMINI_MODEL=gemini-2.5-flash
